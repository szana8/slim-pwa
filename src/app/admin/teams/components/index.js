import Vue from 'vue'

export const Teams = Vue.component('Teams', require('./Teams.vue'))
export const TeamTable = Vue.component('team-table', require('./Table.vue'))
export const TeamForm = Vue.component('team-form', require('./Form.vue'))